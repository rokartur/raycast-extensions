interface:
  display_name: "Raycast Extensions"
  short_description: "Repo-specific patterns and workflows for raycast-extensions"
  default_prompt: "Use the raycast-extensions repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true